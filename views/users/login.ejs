<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Login - Dream Places</title>

<!-- Bootstrap CSS -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
<!-- Google Fonts -->
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
<!-- Font Awesome -->
<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">

<style>
body {
  font-family: 'Poppins', sans-serif;
  margin: 0;
  min-height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  background-color: #0D1F1E; /* Deep luxury greenish background */
  overflow: hidden;
  position: relative;
  color: #fff;
}

/* Floating subtle glows for luxury effect */
.glow {
  position: absolute;
  border-radius: 50%;
  background: rgba(255, 215, 0, 0.15);
  filter: blur(100px);
  pointer-events: none;
  animation: drift 25s infinite linear;
}
.glow:nth-child(1) { width: 250px; height: 250px; top: 10%; left: 15%; }
.glow:nth-child(2) { width: 300px; height: 300px; bottom: 10%; right: 10%; animation-duration: 30s; }
.glow:nth-child(3) { width: 150px; height: 150px; top: 50%; left: 60%; animation-duration: 28s; }

@keyframes drift {
  0% { transform: translateY(0) translateX(0); opacity: 0.1; }
  50% { transform: translateY(-50px) translateX(50px); opacity: 0.3; }
  100% { transform: translateY(0) translateX(0); opacity: 0.1; }
}

/* Glass login card */
.login-card {
  position: relative;
  z-index: 1;
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(15px);
  border-radius: 25px;
  padding: 50px 30px;
  width: 100%;
  max-width: 400px;
  text-align: center;
  box-shadow: 0 15px 50px rgba(0,0,0,0.5);
  border: 1px solid rgba(255,215,0,0.2);
}

/* Heading */
.login-card h2 {
  font-weight: 700;
  margin-bottom: 30px;
  font-size: 2rem;
  letter-spacing: 1px;
  background: linear-gradient(90deg, #FFD700, #FFA500);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text; /* For Firefox support */
  color: transparent;    /* fallback for other browsers */
}


/* Inputs */
.form-control {
  border-radius: 50px;
  padding: 12px 45px 12px 15px;
  background: rgba(255,255,255,0.1);
  border: 1px solid rgba(255,255,255,0.1);
  color: #fff;
  margin-bottom: 20px;
  transition: all 0.3s ease;
}
.form-control::placeholder { color: rgba(255,255,255,0.6);}
.form-control:focus {
  background: rgba(255,255,255,0.15);
  box-shadow: 0 0 10px #FFD700, 0 0 20px #FFA500;
  border: 1px solid #FFD700;
  color: #fff;
}
.input-icon {
  position: absolute;
  top: 50%;
  right: 18px;
  transform: translateY(-50%);
  color: #FFD700;
}

/* Button */
.btn-login {
  border-radius: 50px;
  width: 100%;
  padding: 12px;
  font-weight: 600;
  font-size: 16px;
  border: none;
  background: linear-gradient(90deg, #FFD700, #FFA500);
  color: #0D1F1E;
  box-shadow: 0 0 15px rgba(255,215,0,0.5), 0 0 25px rgba(255,165,0,0.5);
  transition: all 0.3s ease;
}
.btn-login:hover {
  transform: translateY(-3px);
  box-shadow: 0 0 25px #FFD700, 0 0 35px #FFA500;
}

/* Link */
.text-center a {
  color: #FFD700;
  font-weight: 500;
  text-decoration: none;
  transition: 0.3s;
}
.text-center a:hover { color: #FFA500; text-decoration: underline; }

.position-relative { position: relative; }
</style>
</head>
<body>

<!-- Floating glows -->
<div class="glow"></div>
<div class="glow"></div>
<div class="glow"></div>

<div class="login-card">
  <h2>Dream Places</h2>
  <% if (error) { %>
    <div class="alert alert-danger bg-danger text-white"><%= error %></div>
  <% } %>
  <form action="/login" method="POST">
    <div class="mb-3 position-relative">
      <input type="email" class="form-control" name="email" placeholder="Email" required>
      <i class="fa-solid fa-user input-icon"></i>
    </div>
    <div class="mb-3 position-relative">
      <input type="password" class="form-control" name="password" placeholder="Password" required>
      <i class="fa-solid fa-lock input-icon"></i>
    </div>
    <button type="submit" class="btn btn-login">Login</button>
  </form>
  <p class="text-center mt-3">Don't have an account? <a href="/signup">Sign Up</a></p>
</div>

</body>
</html>