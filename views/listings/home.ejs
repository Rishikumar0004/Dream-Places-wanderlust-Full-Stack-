
<!-- <body>
    <h1> WELCOME TO YOUR DREAM PLACES / WANDERLUST.. ❤️ </h1>
</body> -->

<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Dream Places - Wanderlust</title>

<!-- Google Fonts -->
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">

<style>
body {
  margin: 0;
  font-family: 'Poppins', sans-serif;
  min-height: 100vh;
  display: flex;
  justify-content: center;
  align-items: flex-start; /* content on top */
  padding-top: 10vh;
  background-color: #0D1F1E;
  overflow: hidden;
  color: #fff;
  text-align: center;
  position: relative;

  /* Full-screen logo background */
  background-image: url('/images/logo.png'); /* replace with your logo path */
  background-size: 30%;       /* slightly bigger */
  background-repeat: no-repeat;
  background-position:bottom;
  background-attachment: fixed;
  opacity: 1;              /* increase visibility */
  mix-blend-mode: lighten;
}

/* Optional overlay to reduce darkness */
body::before {
  content: '';
  position: absolute;
  top: 0; left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0,0,0,0.45); /* reduce overlay opacity */
  z-index: 0;
}

/* Floating glows - reduce intensity */
.glow {
  position: absolute;
  border-radius: 50%;
  background: rgba(255, 215, 0, 0.05); /* less intense */
  filter: blur(80px); /* slightly less blur */
  animation: float 35s infinite linear;
}
.glow:nth-child(1) { width: 300px; height: 300px; top: 10%; left: 10%; }
.glow:nth-child(2) { width: 250px; height: 250px; bottom: 15%; right: 15%; animation-duration: 40s; }
.glow:nth-child(3) { width: 150px; height: 150px; top: 50%; left: 60%; animation-duration: 45s; }

/* Tiny stars */
.star {
  position: absolute;
  width: 2px;
  height: 2px;
  background: #FFD700;
  border-radius: 50%;
  opacity: 0.6; /* less prominent */
  animation: twinkle 4s infinite alternate;
}

@keyframes float {
  0% { transform: translateY(0) translateX(0);}
  50% { transform: translateY(-50px) translateX(50px);}
  100% { transform: translateY(0) translateX(0);}
}
@keyframes twinkle {
  from { opacity: 0.3; transform: scale(1); }
  to { opacity: 0.8; transform: scale(1.5); }
}

/* Hero content */
.hero-content {
  position: relative;
  z-index: 1;
}

h1 {
  font-size: 3rem;
  font-weight: 700;
  background: linear-gradient(90deg, #FFD700, #FF7E5F, #FFB347);
  background-clip: text;           /* Standard property */
  -webkit-background-clip: text;   /* Webkit browsers */
  color: transparent;              /* fallback for non-supporting browsers */
  -webkit-text-fill-color: transparent; /* Webkit */
  text-shadow: 2px 2px 20px rgba(0,0,0,0.5);
  animation: fadeIn 2s ease forwards;
  letter-spacing: 1px;             /* optional: make it luxury style */
  line-height: 1.2;                /* better vertical spacing */
}


p {
  font-size: 1.3rem;
  font-weight: 500;
  margin-top: 20px;
  color: rgba(255,255,255,0.9); /* slightly more visible */
  animation: fadeIn 2s ease forwards;
  animation-delay: 0.5s;
}

/* Buttons */
.btn-hero {
  margin: 15px 10px 0 10px;
  padding: 12px 30px;
  font-size: 1rem;
  font-weight: 600;
  border-radius: 50px;
  border: none;
  cursor: pointer;
  transition: all 0.3s ease;
  background: linear-gradient(90deg, #FFD700, #FF7E5F);
  color: #0D1F1E;
  box-shadow: 0 5px 15px rgba(255,215,0,0.4), 0 0 25px rgba(255,126,95,0.4);
}
.btn-hero:hover {
  transform: translateY(-3px);
  box-shadow: 0 0 35px #FFD700, 0 0 45px #FF7E5F;
}

/* Fade-in animation */
@keyframes fadeIn {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}
</style>
</head>
<body>

<!-- Floating glows -->
<div class="glow"></div>
<div class="glow"></div>
<div class="glow"></div>

<!-- Stars -->
<div class="star" style="top:20%; left:30%;"></div>
<div class="star" style="top:50%; left:70%;"></div>
<div class="star" style="top:80%; left:40%;"></div>
<div class="star" style="top:35%; left:60%;"></div>
<div class="star" style="top:60%; left:20%;"></div>

<div class="hero-content">
  <h1>WELCOME TO YOUR DREAM PLACES / WANDERLUST ❤️</h1>
  <p>Explore the world. Discover your dream destinations. Adventure awaits!</p>
  <div>
    <button class="btn-hero" onclick="location.href='/listings'">Explore Listings</button>
    <button class="btn-hero" onclick="location.href='/listings/new'">Add Your Place</button>
  </div>
</div>

</body>
</html>
